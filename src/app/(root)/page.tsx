import { Button } from "@/components/ui/button";

export const metadata = {
  title: "Home",
};

export default function Home() {
  return (
    <main>
      <Button>hello</Button>
    </main>
  );
}
