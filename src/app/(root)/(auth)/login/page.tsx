import AuthRightContainer from "@/components/auth/authRightContainer/auth-right-container";

export const metadata = {
  title: "Login",
};

export default function Login() {
  return <AuthRightContainer mode="login" />;
}
